package block

import "github.com/fastygo/base/utils"

templ Block(props BlockProps) {
	switch utils.ResolveTag(props.Tag, "div", utils.TagGroupLayout) {
		case "section":
			<section class={ props.Class } { blockSpreadAttrs(props)... }>{ children... }</section>
		case "article":
			<article class={ props.Class } { blockSpreadAttrs(props)... }>{ children... }</article>
		case "aside":
			<aside class={ props.Class } { blockSpreadAttrs(props)... }>{ children... }</aside>
		case "header":
			<header class={ props.Class } { blockSpreadAttrs(props)... }>{ children... }</header>
		case "footer":
			<footer class={ props.Class } { blockSpreadAttrs(props)... }>{ children... }</footer>
		case "main":
			<main class={ props.Class } { blockSpreadAttrs(props)... }>{ children... }</main>
		case "nav":
			<nav class={ props.Class } { blockSpreadAttrs(props)... }>{ children... }</nav>
		case "figure":
			<figure class={ props.Class } { blockSpreadAttrs(props)... }>{ children... }</figure>
		default:
			<div class={ props.Class } { blockSpreadAttrs(props)... }>{ children... }</div>
	}
}
